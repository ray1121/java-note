* **重写：**
    * 重写方法是不能降低方法的访问权限
    * 参数必须相同，且返回值类型要兼容

* **重载：**
    * **重载不能够只改变返回值类型**

* **多态**
    * **关键词：** 指向子类、父类的引用、向上转型、重写
    * `Wine w = new XO()`
    * **特点:** 只能访问父类中拥有的方法和属性，运行时调用，当子类中重写父类的方法时，调用的就是子类的方法，**对于父类中没有的方法或者子类重载的父类的方法，该引用是不能调用的**

